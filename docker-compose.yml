version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./:/app:rw
    environment:
      - FLASK_ENV=development
      - PROJECTS_DB=/app/projects.db
    command: gunicorn -b 0.0.0.0:5000 app:app
